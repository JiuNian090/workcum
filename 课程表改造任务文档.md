# 课程表项目改造任务文档

## 项目概述
将现有的工时记录排班系统改造为课程表管理系统，保持整体UI设计不变，包括按钮、弹窗、卡片等组件样式，仅调整功能逻辑和数据结构。

## 功能需求

### 1. 三个核心界面
项目将包含三个主要界面："课程"、"课表"、"我的"

#### 1.1 "课程"界面
- 功能定位：课程模板管理界面
- 核心功能：
  - 支持添加自定义课程
  - 课程信息包括：
    - 课程名称
    - 导师
    - 颜色选择
  - 与原"班次模板管理界面"功能类似

#### 1.2 "课表"界面
- 功能定位：周历视图课表
- 核心功能：
  - 第一行显示：
    - 第几周
    - 周几、日期（月-日）
  - 左侧显示时间阶段：
    - 共12段时间
    - 每4段为一组
    - 最左侧一列为序号列，支持自定义时间，各时间段支持自定义起止时间
  - 时间阶段与日期交叉格子：
    - 支持点击添加课程
    - 点击后弹出表单弹窗，包含：
      - 课程模板选择
      - 地点字段
  - 课程显示逻辑：
    - 每天的各个时间段只能添加一个课程
    - 当一组中的两个时间段课程选择和地点选择相同时，两个格子的颜色会联合合并成一个格子

#### 1.3 "我的"界面
- 功能定位：个人中心与数据管理
- 核心功能：
  - 项目介绍功能
  - 数据导出功能
  - 分享功能
  - 数据导入功能
  - 数据清除功能

## 技术实现计划

### 第一阶段：项目结构调整
1. 重命名现有页面组件以匹配新功能
2. 调整路由配置
3. 更新导航菜单文字
4. 修改本地存储键名

### 第二阶段：课程模板功能开发
1. 创建课程模板数据结构
2. 实现课程模板的增删改查功能
3. 开发课程模板表单组件
4. 添加颜色选择器功能

### 第三阶段：课表界面开发
1. 设计12段时间显示组件
2. 实现周历视图布局
3. 开发课程添加功能
4. 创建课程表单弹窗

### 第四阶段："我的"界面优化
1. 添加项目介绍内容
2. 保留现有数据管理功能
3. 优化界面布局

## 数据结构变更

### 课程模板数据结构
```javascript
{
  id: string,
  name: string,        // 课程名称
  instructor: string,  // 导师
  color: string,       // 颜色
  createdAt: Date
}
```

### 课表数据结构
```javascript
{
  id: string,
  date: string,        // 日期
  timeSlot: number,    // 时间段(1-12)
  courseId: string,    // 课程模板ID
  location: string,    // 地点
  createdAt: Date
}
```

## UI/UX 设计要求

### 保持现有设计元素
- 继续使用现有的按钮样式
- 保持弹窗设计风格一致
- 卡片组件样式不变
- 响应式设计适配移动端

### 新增设计元素
- 时间段分组视觉标识
- 课程颜色标识系统
- 周数显示组件

## 测试计划

### 功能测试
1. 课程模板创建、编辑、删除功能测试
2. 课表添加、编辑、删除功能测试
3. 数据导入导出功能测试

### 兼容性测试
1. 桌面端浏览器兼容性测试
2. 移动端适配测试
3. 不同屏幕尺寸响应式测试

## 风险评估与应对

### 技术风险
- 数据迁移可能丢失原有数据
  - 应对：开发前进行完整备份
- 时间段自定义功能复杂度高
  - 应对：分步骤实现，先实现固定时间段

### 用户体验风险
- 界面文字变更可能造成用户困惑
  - 应对：提供清晰的引导说明
- 功能逻辑调整需要用户适应
  - 应对：保持操作流程一致性

## 交付标准
1. 所有三个界面功能完整可正常使用
2. UI设计符合要求，保持原有风格
3. 数据结构合理，便于后续扩展
4. 通过所有测试用例
5. 提供必要的使用说明文档